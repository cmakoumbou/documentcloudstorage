<% provide(:title, 'All documents') %>
<h1>All documents</h1>
<% for document in @documents %>
	<li>
		<%= link_to document.uploaded_file_file_name, document %>
		<% if current_user?(document.user) %>
			<%= link_to "delete", document, method: :delete,
											data: { confirm: "You sure?"},
											title: document.uploaded_file_file_name %>
		<% end %>
	</li>
<% end %>